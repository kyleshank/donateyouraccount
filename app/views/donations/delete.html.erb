<div class="page-header">
  <h1><%=@donation.campaign.name%> <small>Delete Donation</small></h1>
</div>
<div class="alert alert-warning">Are you sure you want to delete this <%= @donation.account.type=="TwitterAccount" ? "Twitter" : "Facebook" %> donation to <%=link_to @donation.campaign.name, campaign_path(@donation.campaign)%>?</div>
<%=button_to "Delete Donation", campaign_donation_path(@campaign,@donation), :class=>"btn btn-danger", :method => :delete%>